using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionTooth : MonoBehaviour
{

    private Main main;

    // Start is called before the first frame update
    private void Awake()
    {
        main = FindObjectOfType<Main>();
    }

    // Update is called once per frame

   
}
